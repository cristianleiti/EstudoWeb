const resultado = nota => nota >= 7 ? 'Nota ' + nota + ' - Aprovado!' : 'Nota ' + nota + ' - Reprovado!'

console.log(resultado(7.1))
console.log(resultado(5.1))

//const status = nota >=7 ? 'Nota '+nota+' Aprovado': 'Nota '+nota+' Reprovado'
